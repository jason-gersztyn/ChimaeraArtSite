using System.Linq;
using Chimaera.Beasts.Integration;
using Chimaera.Beasts.Model;
using Chimaera.Beasts.Service;
using Chimaera.Beasts.Utils;

namespace Chimaera.Junk
{
    class Program
    {
        static void Main(string[] args)
        {
            //PrintAura.ListMyImages();
            //var products = ProductService.GetProducts();
            //foreach(var product in products)
            //    PrintAura.ListProducts(product);
            //PrintAura.GetColors();
            //Series series = SeriesService.GetSeries(SeriesID: 2).FirstOrDefault();
            //series.Products = ProductService.GetProducts(SeriesID: series.SeriesID).ToArray();
            //foreach(Product product in series.Products)
            //{
            //    PrintAura.ListSizes(product.ProductID, product.PrintAuraID, product.Color.PrintAuraID);
            //}
        }
    }
}